{
  "name": "function-starter",
  "version": "1.0.0",
  "description": "Boilerplate for TS cloud functions",
  "main": "index.js",
  "author": "Preston Holmes <preston@ptone.com>",
  "license": "Apache-2.0",
  "dependencies": {
    "@types/express": "^4.16.1",
    "@types/node": "^11.13.6",
    "express": "^4.16.4",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.4"
  },
  "devDependencies": {
    "gts": "^0.9.0",
    "typescript": "~3.1.0"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "deploy": "gcloud beta functions deploy --runtime nodejs10 --trigger-http --source ./build"
  }
}
